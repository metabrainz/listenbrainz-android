package org.listenbrainz.android.ui.screens.profile.playlists

enum class PlaylistType(val title: String) {
    USER_PLAYLIST("Playlist"),
    COLLABORATIVE("Collaborative")
}

enum class PlaylistView{
    LIST,
    GRID
}